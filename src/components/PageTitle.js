import React from "react";
import "../assets/styles/PageTitle.css";

export default function Regionais({ title }) {
   return (
      <div>
         <div className="page-title">{title}</div>
      </div>
   );
}
